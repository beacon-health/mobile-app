package_name("org.beaconhealth.app")

# Play service-account JSON — never stored in the repo; supplied by CI as the
# PLAY_SERVICE_ACCOUNT_JSON secret (the whole JSON document, not a path).
json_key_data(ENV["PLAY_SERVICE_ACCOUNT_JSON"])
